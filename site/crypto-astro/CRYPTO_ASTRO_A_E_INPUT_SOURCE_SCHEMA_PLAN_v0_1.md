# Crypto-Astro A/E Input Source Schema Plan v0.1

STATUS: PLAN_ONLY
PUBLIC_SURFACE: Crypto-Astro
REPO: AiBhrigu/phi-cosmography-open
BOUNDARY: CLEAN

## Purpose

Define A/E input source schema semantics as a repo-visible documentation artifact without opening numeric A/E activation, production schema changes, data changes, runtime ingestion, backend/API, source adapters, or public HTML changes.

## Current state

A/E input source schema is a local artifact closed state now made visible as a plan-only repo artifact.

A/E UI semantics remains parked until the exact UI-copy patch route closes.

## Source class semantics

A input means astro-context source class.

E input means ephemerides / evidence source class.

Both classes remain pending and calibration-only.

They do not have current scoring effect.

They do not activate formula weights.

They do not imply live source ingestion.

## Source mode

Initial source mode must remain static public artifact.

Live adapters are closed until a separate adapter plan, boundary review, and validation route are authorized.

## Suggested future field names

Future documentation or schema design may use neutral source-class naming:

- a_source_class_state
- e_source_class_state
- a_source_mode
- e_source_mode
- a_calibration_state
- e_calibration_state
- a_boundary
- e_boundary

Recommended neutral values:

- pending
- calibration_pending
- static_public_artifact
- context_only
- inactive
- no_current_effect

## Blocked interpretations

A/E source classes must not be described as active signals.

A/E source classes must not be described as source adapters.

A/E source classes must not be described as runtime ingestion.

A/E source classes must not be folded into active scoring until a later formula validation node.

## Separation from active layers

A/E source schema must remain separate from:

- M market context
- TVL / liquidity context
- Φ Balance
- Φ Resonance
- Sem Phase
- public-safe output language
- runtime/backend/API

## Dependency lock

A/E UI semantics repair remains a separate parked track.

This artifact does not repair UI copy.

This artifact does not change site/crypto-astro/index.html.

A/E numeric activation remains closed until:

1. A/E UI semantics is clean.
2. Repo-visible A/E source schema is reviewed.
3. Historical formula validation is opened and passed.
4. Boundary review explicitly authorizes the next step.

## Validation checklist

Before any future A/E source schema production change, confirm:

- changed files match authorized scope
- public HTML is untouched unless explicitly authorized
- data JSON is untouched unless explicitly authorized
- production schema JSON is untouched unless explicitly authorized
- runtime/backend/API remain closed
- no live adapter claim is added
- no active A/E scoring is added
- no public financial action language is added
- protected ORION internals are not exposed
- boundary remains clean

## Current decision

This plan only exposes the A/E source schema semantics contour as documentation.

It does not activate A/E numerics.

It does not create or change a production JSON schema.

It does not create data feeds.

It does not alter the public surface.

## Boundary

PUBLIC_HTML_CHANGE=NO
SCHEMA_IMPLEMENTATION=NO
DATA_CHANGE=NO
RUNTIME=NO
BACKEND_API=NO
A_E_ACTIVATION=NO
SOURCE_ADAPTERS=NO
LIVE_INGESTION=NO
PUBLIC_FINANCIAL_ACTION_LANGUAGE=NO
BOUNDARY=CLEAN
