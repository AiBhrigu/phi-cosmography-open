# Crypto-Astro Module Connection Map v0.1

STATUS: MODULE_CONNECTION_MAP
PUBLIC_SURFACE: Crypto-Astro
REPO: AiBhrigu/phi-cosmography-open
BOUNDARY: CLEAN

## Purpose

Define the current module connection contour for Crypto-Astro without opening implementation, runtime, backend/API, TVL, or A/E numeric activation.

## Governance anchor

All public-surface or production changes follow:

branch -> patch -> verify diff -> draft PR -> validation -> merge -> public verify -> closure report -> memory fixation

Governance source:

- site/crypto-astro/GOVERNANCE_PR_VALIDATION_SUITE_v0_1.md

## Active module map

### 1. Public Surface

State: live / locked.

Current public surface shows Crypto-Astro service route, scoring preview, public-safe copy, market context active, and A/E pending semantics.

Allowed connection:

- Governance validation only
- Public-safe documentation only
- UI copy patch only after bounded branch / PR route

Blocked connection:

- direct main patch
- backend/API
- runtime
- protected ORION core exposure
- trading signal / forecast / investment recommendation

### 2. Governance

State: complete.

Artifact on main:

- site/crypto-astro/GOVERNANCE_PR_VALIDATION_SUITE_v0_1.md

Role:

- gate public-surface changes
- enforce PR packet
- enforce changed-files proof
- enforce boundary checks
- preserve closure and memory fixation route

### 3. A/E Source Semantics

State: parked.

Truth anchor:

- Issue #38
- Fresh branch: crypto-astro-a-e-source-semantics-fresh-v0-1
- Local atompack handoff posted

Current blocker:

- exact patch-capable route required for site/crypto-astro/index.html
- no confirmed worker result yet

Allowed next connection:

- worker/local atom applies exact UI-copy patch on branch
- verify changed file count
- draft PR only after patch result

Blocked connection:

- full-file replacement of index.html
- A/E numeric activation
- schema/data change
- live adapter claim

### 4. A/E Input Source Schema

State: local artifact closed / repo patch not opened.

Role:

- define A input and E input as pending source classes
- preserve source mode as static public artifact
- keep calibration pending until formula review

Allowed next connection:

- repo artifact branch only after A/E UI semantics is repaired or explicitly parked

Blocked connection:

- numeric A/E scoring
- source adapter claims
- runtime ingestion

### 5. Module Automation Chain

State: local chain pass / passive.

Known chain:

- intake packet automation
- boundary filter automation
- operator packet generator
- ephemerides precompute
- snapshot draft generator
- archive hash automation
- research feed boundary

Role:

- support manual-service workflow
- remain local/passive until authorized

Blocked connection:

- runtime daemon
- backend/API
- public feed
- subscription/payment activation

### 6. TVL / Liquidity Context Layer

State: plan only / not opened.

Role:

- future context layer for liquidity and capital environment

Allowed next connection:

- semantics plan
- schema separation plan
- boundary review

Blocked connection:

- implementation
- live adapter
- investment signal
- forecast

### 7. Historical Formula Validation

State: later / not opened.

Role:

- evaluate formulas against historical context after semantics and governance are stable

Blocked connection:

- live scoring change
- A/E activation
- public prediction claims

### 8. Runtime / Backend / API

State: closed.

Role:

- no active connection in current public surface contour

Blocked connection:

- runtime daemon
- backend/API route
- live search adapter
- live social adapter
- private client data flow

## Connection rules

1. Public surface only receives changes through governance PR route.
2. A/E source semantics must close before A/E numeric activation can even be reviewed.
3. A/E input schema remains static/public and pending until formula review.
4. TVL/liquidity stays plan-only until a separate boundary and schema plan exists.
5. Automation modules stay passive unless runtime authorization is explicit.
6. Runtime/backend/API remain closed.
7. No trading signal, forecast, price target, or investment recommendation language may enter public surface.
8. Protected ORION internals are not exposed.

## Current parked threads

- A/E source semantics exact patch: parked pending worker/local patch result.
- TVL/liquidity context: plan-only.
- Historical formula validation: later.
- Runtime/backend/API: closed.

## Next safe route

After this artifact:

1. Open draft PR for module connection map.
2. Validate changed files.
3. Merge only if documentation-only and boundary clean.
4. Return to A/E source semantics when worker/local atom result appears.

## Boundary

PUBLIC_HTML_CHANGE=NO
SCHEMA_CHANGE=NO
DATA_CHANGE=NO
RUNTIME=NO
BACKEND_API=NO
A_E_ACTIVATION=NO
TVL_IMPLEMENTATION=NO
TRADING_SIGNAL=NO
FORECAST=NO
INVESTMENT_RECOMMENDATION=NO
BOUNDARY=CLEAN
